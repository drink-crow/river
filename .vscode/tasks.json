{
    "tasks": [
        {
            "type": "cppbuild",
            "label": "Build CMake",
            "command": "cmake",
            "args": [
                "--build",
                "build",
                "--config",
                "Debug",
                "--target",
                "all"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "CMake 构建任务。"
        }
    ],
    "version": "2.0.0"
}